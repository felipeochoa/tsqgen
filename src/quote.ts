export const identifier = (ident: string) => ident;
export const literal = (lit: unknown) => lit + '';
export const tableName = (name: string) => name;
export const columnName = (col: string) => col;
export const operator = (op: string) => op;
